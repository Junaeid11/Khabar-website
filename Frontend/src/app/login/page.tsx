import LoginForm from "@/components/modules/auth/login/LoginForm";

const LoginPage = () => {
  return (
    <div className="h-screen w-screen backdrop-brightness-90 flex justify-center items-center">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
